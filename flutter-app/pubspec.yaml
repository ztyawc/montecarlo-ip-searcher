name: mcis
description: MCIS Android IP searcher with a Flutter and miuix interface.
publish_to: none
version: 0.4.1+5
environment:
  sdk: '>=3.12.2 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  flutter_miuix: 1.1.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
